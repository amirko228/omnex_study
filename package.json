{
      "name": "ai-learning-platform-root",
      "version": "0.1.0",
      "private": true,
      "scripts": {
            "postinstall": "cd nextjs-app && npm install",
            "dev": "npm run dev --prefix nextjs-app",
            "build": "npm run build --prefix nextjs-app",
            "start": "npm run start --prefix nextjs-app",
            "lint": "npm run lint --prefix nextjs-app",
            "test": "npm run test --prefix nextjs-app"
      },
      "dependencies": {
            "@types/nodemailer": "^7.0.10",
            "nodemailer": "^8.0.1"
      }
}
